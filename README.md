# Iron Music 420 AI

Flutter Android app for the Iron Music 420 AI Bulgarian voice/chat assistant.

## Current main build

- Version: `3.4.0+62`
- Build name: `v3.4.0 build 62 real 3D core`
- APK artifact: `iron-music-420-ai-v3.4.0-build62-real-3d-core`

## Build 62 real 3D core

- Replaces the flat HUD PNG core body with a code-drawn cannabis leaf inside the glass sphere.
- Keeps one visible leaf and aligns the pulsing vein layer to the same leaf geometry.
- Removes HUD wording from the chat transition so the first screen reads as the core, not a control panel.
- Preserves the fixed Bulgarian voice path and the stable signed Android package.

## Build 61 3D core aligned veins

- Clips the pulsing vein overlay to the cannabis leaf shape instead of the whole orb.
- Makes the vein pulse thinner and internal so it does not cross empty space.
- Adds stronger glass depth, inner shadow, rim lift and lower sphere shading.
- Keeps the existing signed Android package so the APK can update the installed app.

## Build 60 clean core interface

- Replaces the HUD control center with a clean living-core first screen.
- Keeps one large glowing cannabis leaf inside the center core.
- Removes the start-screen command grid, status cards and bottom tabs.
- Leaves only chat, microphone and voice controls around the core.
